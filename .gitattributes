# golden 원본은 바이트 그대로 보존한다. writer가 boilerplate를 복제하므로
# git이 줄바꿈을 바꾸면 클론마다 .scnx 산출 바이트가 달라진다.
yewon_test/* -text

# 시나리오 원문도 그대로. 파서가 줄 단위로 읽고 게이트가 줄 번호를 보고한다.
scenario_original/scenario_v3.txt -text
